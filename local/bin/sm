#!/bin/bash

file=`fd --full-path ~/manuals/ /home/nikin | fzf`

mime=$(xdg-mime query filetype "$file")

if [[ "${mime}" == text/* && "${file}" != *.html ]]; then
  /usr/bin/nvim "${file}"
elif [[ -n "${file}" ]]; then
  xdg-open "${file}" &
else
  :
fi
