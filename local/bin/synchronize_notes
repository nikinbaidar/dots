#!/usr/bin/env bash

set -e

cd /home/nikin/notes

git pull

if [[ -n "$(git status -s)" ]]
then
    git add --all
    git commit -m "syncnotes $(date '+%Y-%m-%d %H:%M')"
    git push origin main
fi

date '+%Y-%m-%d %H:%M' > _date.txt
